#include <stdio.h>

/**
 * main - Entry point of the program
 *
 * Descriptiont:writtet all number
 *
 * Retrun: Always 0 (success)
 */

int main(void)
{
	int b = 48;

	while (b <= 57)
	{
		putchar(b);
		b++;
	}
	putchar('\n');
	return (0);
}
