#include <stdio.h>

/**
 * main - Entry point oh the program
 *
 * Description:writtet all number
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int b = 0;

	while (b < 10)
	{
		prinft("%d", b);
		b++;
	}
	printf("\n");
	return (0);
}
