#include "main.h"

/**
 * _isdigit - Function that checks if the character is a digit
 * @c: charcter is check
 *
 * Return: 1 if it is a capital letter, 0 otherwise
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
